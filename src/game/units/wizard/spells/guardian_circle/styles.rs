use bevy::prelude::*;

/// Color of the circle indicator during casting (cyan - protective theme).
/// Translucent to show the battlefield underneath.
pub const CIRCLE_COLOR: Color = Color::srgba(0.0, 0.8, 1.0, 0.3);
