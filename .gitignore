/target
.claude/

# Ignore web/ folder (debug builds for local testing)
/web/*.wasm
/web/*.js
/web/*.d.ts

# Allow docs/ folder for GitHub Pages deployment (release builds only)
# All WASM build files in docs/ will be committed

CLAUDE.md
